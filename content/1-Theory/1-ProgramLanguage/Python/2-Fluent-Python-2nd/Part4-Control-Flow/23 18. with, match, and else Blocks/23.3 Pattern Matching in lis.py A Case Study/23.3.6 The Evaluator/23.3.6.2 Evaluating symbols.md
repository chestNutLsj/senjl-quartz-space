### Evaluating symbols

    `case` `Symbol``(``var``):`
        `return` `env``[``var``]`

Subject:

Instance of `Symbol`, i.e., a `str` used as an identifier.

Action:

Look up `var` in `env` and return its value.

Examples:

```
>>> 
```